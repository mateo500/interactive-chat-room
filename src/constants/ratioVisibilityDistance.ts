export enum RatioVisibilityDistance {
  NEAR = "0.9",
  MEDIUM = "0.6",
  FAR = "0.3",
  NO_VISIBLE = "0",
}

export enum DistanceConstrains {
  NEAR = 150,
  MEDIUM = 500,
  FAR = 1000,
  NO_VISIBLE = 1300,
}

export const MAX_VISIBLE_DISTANCE = 1310;
